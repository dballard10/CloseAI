# --- core ---
pydantic>=2.6

# --- observability (sponsor: Weights & Biases) ---
weave>=0.51

# --- agent 1: deterministic PII detection ---
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7
# install the model separately:  python -m spacy download en_core_web_lg

# --- agent 2: local contextual detection via Ollama ---
requests>=2.31

# --- closed-model clients (install what you use) ---
openai>=1.30
anthropic>=0.25

# --- web demo ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- tests ---
pytest>=8.2
